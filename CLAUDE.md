# Foundation Project Guidelines

## UI / Table Standards

- **All number fields must be right-aligned in tables.** Use `meta: { align: "right" }` on DataTable column definitions, or `text-right tabular-nums` classes on hand-rolled `<td>` elements.
